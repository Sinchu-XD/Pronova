[phases.setup]
aptPkgs = ["git", "curl", "nodejs", "python3-pip", "pipx"]

[phases.install]
cmds = [
  "pip install --break-system-packages -r requirements.txt",
  "pipx install yt-dlp[default] || true",
  "pipx ensurepath || true"
]
